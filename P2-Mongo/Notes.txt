NOSQL Booster
systemctl start docker
Run mongo server:
    sudo docker -compose up -d (opcional daemon)
Run mongosh
    docker-compose exec mongoDB mongosh

Install mongo -> the installation of mongo in my case through "yay -S mongo" and it's tools can take up to 180gb compiling and 7 hours do finish
I canceled it


Docker ps -> to see active containers


pass: 5248
template: checkout ...

Consegue-se aplicar operadores nos queries na versao mais crude
Podemos aplicar uma api de agregações
